# crash-severity-prediction
End-to-end machine learning project to predict crash severity using FARS dataset, handling class imbalance with SMOTE and achieving high recall for multi-fatal crash detection.
